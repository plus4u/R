# install.packages

corrplot # 

doBy # by-sample

ltm # linear regression

## 

## pca analysis

# install.packages("GPArotation")

install.packages("psych")

library(psych)
library(GPArotation)
library(readxl)

psych #

GPArotation #

library("factoextra") # pca 

library(shinythemes) # style 

# install.packages("rhandsontable")

install.packages('shinyjs')

# install.packages("car")  # for multicollinearity
library(car)

library(nFactors) # principle plot

library(lm.beta) # standard beta

library(QuantPsyc) # lm or beta 

library(MASS) # lda 



